{{!-- 

    Specific comment on specific ride

    /homepage/{idOfThemePark}/rides/{idOfRide}/comments

    - list of comments
    - add button
 --}}

  <h1>Comments on Rides</h1>

 <main>
    <ol>
        {{#each comments}}
        <li>
            {{userName}}
            <br>
            Comment: {{commentBody}}
        </li>
        {{/each}}
    </ol>
</main>

<a href = '/themepark/{{_id}}/rides/{{_rideId}}/addComment'>Add comment on ride</a>