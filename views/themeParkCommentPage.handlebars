{{!-- 
Theme Park Comment Page (INDIVIDUAL THEME PARK)

    /homepage/{idOfThemePark}/comments
- list of all comments
- add button (Another page)
 --}}

<a href = "/themepark/{{_id}}">Go back to theme park page</a>
<br>
<p id = "error"></p>
<button id="addThemeParkComment">Add Theme Park Comment</button>
<div id="comment" hidden>
    <p>Please enter the following information to add a comment to a theme park</p>
    <form id = "commentForm">
        <label for="theme_park_comment">Theme Park Comment</label>
        <br>
        <textarea name="theme_park_comment" id="theme_park_comment"></textarea>
        <br>
        <button data-id={{_id}} type="submit">Submit comment</button>
    </form>
    <button id="cancelButton">Cancel</button>
</div>
<div id="commentList">
  {{#each comments}}
    <div data-id="{{_id}}">
      User: {{userName}} Comment: {{commentBody}}
      <button class="childCommentBtn">Add a comment</button>
      <div hidden>
        <p>Reply</p>
        <form class="childCommentForm">
          <label for="childCommentForm"></label>
          <br>
          <textarea name="childCommentBody" class="childCommentBody"></textarea>
          <br>
          <button  type="submit">Submit Reply</button>
        </form>
        <button class="cancelChildButton">Cancel</button>
      </div>
      <br>
      {{#each comments}}
        <p>{{commentBody}}</p>
      {{/each}}
    </div>
  {{/each}}
</div>

