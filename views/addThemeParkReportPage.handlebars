<h1> Report Theme Park - {{themeParkName}} </h1>
<form id="reportThemeParkForm" method="POST" action="/themepark/{{themeId}}/addReport">
    <label for="report_reason">Reason for reporting:</label>
    <textarea name="report_reason" id="report_reason" required></textarea>
    <button type="submit">Submit Report</button>
</form>
